/**
Add two numbers.

@param x Fist number.
@param y Second number.
@return The sum.
*/
real add(real x,
         real y) {
    return x + y;
}


/**
Subtract two numbers.

@param x Fist number.
@param y Second number.
@return The difference.
*/
real subtract(
    real x,
    real y
) {
    return x - y;
}
